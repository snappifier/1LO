
export const Navbar = () => {

    return(<>
            <span className="text-blue-600"></span>
    {/*<header className="fixed w-full bg-gray-0 rounded-md bg-clip-padding backdrop-filter backdrop-blur-sm bg-opacity-30 h-20">*/}
        <header className="sticky top-0 left-0 z-50 w-full h-20 inset-0 backdrop-blur-none bg-black/65">
            <nav>

            </nav>
        </header>
        </>
    );
}